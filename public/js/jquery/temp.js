(function ($) {
    $.fn.temp = function (options) {
        let _this = this;

        let defaults = {
        };
        let settings = $.extend({}, defaults, options);

        _this.init = function () {
            //code here
        };

        return _this.init();
    };
})(jQuery);

